{
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --outdir=dist && tailwindcss -i src/styles.css -o dist/styles.css --minify",
    "watch": "esbuild src/app.js --bundle --minify --outdir=dist --watch & tailwindcss -i src/styles.css -o dist/styles.css --watch"
  },
  "devDependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "daisyui": "^5.5.19",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.2.1"
  }
}
