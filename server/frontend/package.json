{
  "private": true,
  "scripts": {
    "build": "esbuild src/terminal.js src/file_manager.js --bundle --minify --outdir=dist && esbuild src/styles.css --bundle --minify --outdir=dist",
    "watch": "esbuild src/terminal.js src/file_manager.js --bundle --minify --outdir=dist --watch & esbuild src/styles.css --bundle --minify --outdir=dist --watch"
  },
  "devDependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "esbuild": "^0.25.0"
  }
}
